import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    item: {
        flexDirection: 'row',
        marginBottom: 5,
        justifyContent: 'center',
    },  

    arrow: {
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
    },

    queue: {
        width: 60,
        height: 60,
        borderWidth: 1,
        borderRadius: 5,
        justifyContent: 'center',
        alignItems: 'center',
    },
})